# grunt-karma-sequence
run karma with multiple browsers sequentially

## Installation
`npm install git://github.com/Malkiz/grunt-karma-sequence --save-dev`

`grunt.loadNpmTasks('grunt-karma-sequence');`
